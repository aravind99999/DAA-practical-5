INPUT 
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int knapsack(int W, const vector<int>& weights, const vector<int>& values, int n) {

    vector<vector<int>> dp(n + 1, vector<int>(W + 1, 0));

    for (int i = 1; i <= n; i++) {
        for (int w = 1; w <= W; w++) {
            
            if (weights[i - 1] <= w) {
                
                dp[i][w] = max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w]);
            } else {
                
                dp[i][w] = dp[i - 1][w];
            }
        }
    }

    return dp[n][W];
}

int main() {
    int n, W;

    cout << "Enter the number of items: ";
    if (!(cin >> n) || n <= 0) {
        cout << "Invalid number of items." << endl;
        return 1;
    }

    cout << "Enter the maximum capacity of the knapsack: ";
    if (!(cin >> W) || W <= 0) {
        cout << "Invalid knapsack capacity." << endl;
        return 1;
    }

    vector<int> values(n);
    vector<int> weights(n);

    cout << "\nEnter the values of the " << n << " items:\n";
    for (int i = 0; i < n; i++) {
        cout << "Value of item " << (i + 1) << ": ";
        cin >> values[i];
    }

    cout << "\nEnter the weights of the " << n << " items:\n";
    for (int i = 0; i < n; i++) {
        cout << "Weight of item " << (i + 1) << ": ";
        cin >> weights[i];
    }

    int max_value = knapsack(W, weights, values, n);

    cout << "\n=========================================" << endl;
    cout << "Maximum value reachable in knapsack = " << max_value << endl;
    cout << "=========================================" << endl;

    return 0;
}


OUTPUT 
Enter the number of items: 4
Enter the maximum capacity of the knapsack: 20

Enter the values of the 4 items:
Value of item 1: 5
Value of item 2: 6
Value of item 3: 9
Value of item 4: 1

Enter the weights of the 4 items:
Weight of item 1: 3
Weight of item 2: 7
Weight of item 3: 8
Weight of item 4: 2

=========================================
Maximum value reachable in knapsack = 21
=========================================



